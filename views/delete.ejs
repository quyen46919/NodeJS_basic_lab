<!DOCTYPE html>
<html>
  <head>
    <title>Xóa bài báo</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <% include components/header %>
    <div
      class="w-7xl max-w-7xl h-auto flex flex-col item-center justify-start m-auto mt-8 mb-10 gap-10 sm:mx-[100px]"
    >
      <h1 class="text-2xl">Xóa bài báo</h1>
      <% if (errorMessage) {%>
      <div
        class="bg-red-100 border border-red-400 text-red-700 px-4 py-4 rounded relative"
        role="alert"
      >
        <span class="block sm:inline"><%= errorMessage %></span>
      </div>
      <%}%>
      <div>
        <p>Bạn có chắc chắn là muốn xóa bài báo này không?</p>
        <span>Tiêu đề: <%= news.title  %> </span>
        <span>Bạn có chắc chắn là muốn xóa bài báo này không?</span>
      </div>
      <form
        class="space-y-5"
        action=<%= `/news/delete?id=${news._id}` %>
        method="post"
      >
        <div>
          <button
            type="submit"
            class="w-[300px] flex justify-center bg-red-400 hover:bg-red-500 text-gray-100 p-3 rounded-sm tracking-wide font-semibold shadow-lg cursor-pointer transition ease-in duration-500"
          >
            Xóa bài báo
          </button>
        </div>
      </form>
    </div>
  </body>
</html>